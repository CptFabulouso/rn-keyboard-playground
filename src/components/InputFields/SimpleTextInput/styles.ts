import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    padding: 5,
    backgroundColor: 'white',
    borderRadius: 2,
  },

  label: {
    fontSize: 15,
  },
});
